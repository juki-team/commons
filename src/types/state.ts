export enum ScopeData {
  USER = 'USER',
  PROBLEM = 'PROBLEM',
  CONTEST = 'CONTEST',
  ATTEMPT = 'ATTEMPT',
}

export enum Status {
  SUCCESS = 'SUCCESS',
  ERROR = 'ERROR',
  LOADING = 'LOADING',
  NONE = 'NONE',
}
