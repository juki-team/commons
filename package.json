{
  "name": "@juki-team/commons",
  "version": "0.1.6",
  "author": "juki-team",
  "license": "MIT",
  "type": "module",
  "main": "dist/cjs/index.cjs",
  "module": "dist/esm/index.js",
  "types": "dist/types/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/types/index.d.ts",
      "import": "./dist/esm/index.js",
      "require": "./dist/cjs/index.cjs"
    }
  },
  "files": [
    "dist"
  ],
  "repository": "https://github.com/juki-team/commons.git",
  "scripts": {
    "docs": "typedoc src/index.ts",
    "watch": "rollup -cw",
    "build": "rm -rf docs && rm -rf dist && rollup -c && yarn docs",
    "git:commit-push": "f() { param=${1}; message=${param:-chore}; echo ${message}; git add .; echo $message | git commit -F -; yarn publish --patch --no-git-tag-version; version=\"v$(node -p \"require('./package.json').version\")\"; git add .; echo $version | git commit -F -; git push origin main; echo $version | git tag -a $(echo $version) -F -; git push origin --tags; }; f",
    "git:push": "f() { yarn publish --patch --no-git-tag-version; version=\"v$(node -p \"require('./package.json').version\")\"; git add .; echo $version | git commit -F -; git push origin main; echo $version | git tag -a $(echo $version) -F -; git push origin --tags; }; f",
    "push-commit": "f() { yarn build && yarn git-push \"${1}\"; }; f",
    "push": "yarn build && yarn git:push"
  },
  "devDependencies": {
    "@types/uuid": "^11.0.0",
    "prettier": "^3.8.1",
    "rollup": "^4.57.1",
    "rollup-plugin-typescript2": "^0.36.0",
    "typedoc": "^0.28.16",
    "typescript": "^5.9.3"
  }
}
